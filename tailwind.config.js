/** @type {import('tailwindcss').Config} */
module.exports = {
  // run all those files that are in src folder with the extension [ .js || .jsx ||  .ts || .tsx ]
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
